class Config:

    # approximate height and width of an image, used for scaling
    dim = 100

    param_types = {
        "n_clusters": int,
        "min_cluster_size": int,
        "min_samples": int,
        "max_eps": float,
    }

